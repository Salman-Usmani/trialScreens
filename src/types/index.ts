import {ITextBox} from './interface';
import {
  bottomTabProp,
  bottomTabScreenList,
  rootStackNavigationProp,
  rootStackScreenList,
} from './types';

export type {
  // stack props
  rootStackScreenList,
  rootStackNavigationProp,
  bottomTabScreenList,
  bottomTabProp,

  // Interfaces
  ITextBox,
};
